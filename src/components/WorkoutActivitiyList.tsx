import React, { useState } from 'react';

import {Activity, Exercise} from '../types/definitions';

function WorkoutActivityList({ activities }: { activities: Activity[] }) {
    
//    const listItems = products.map(product =>
//        <li key={product.id}>
//            {product.title}
//        </li>
//    );
//    
//    return (
//        <ul>{listItems}</ul>
//    );
    
    return (
        <section>
        </section>
    );
}

export default WorkoutActivityList;
